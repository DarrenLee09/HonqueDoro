{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "routes": [
    {
      "src": "/((?!api).*)$",
      "dest": "/index.html"
    }
  ]
}